export const ERRORS = {
  MISSING_BIRTHDATE:
    "The birthdate query parameter is required and must follow the format YYYY-MM-DD",
  INVALID_FORMAT: "birthdate format must be YYYY-MM-DD",
  INVALID_DATE: "birthdate must be a valid date",
};
